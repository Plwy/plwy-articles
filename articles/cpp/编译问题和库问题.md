# 编译问题和库问题

## 程序的编译过程

代码编译成可执行程序的过程，主要分为:预编译(Prepressing)->编译(Compilation)->汇编(Assembly)->链接(Linking)

- 预编译(Prepressing)
  将所有的"#define"删除，并且展开所有的宏定义
  处理所有条件预编译指令，比如"#if",“#ifdef”,“#elif”,“#else”,“endif”
  处理 “#include” 预编译指令，将被包含的文件插入到该预编译指令的位置，注意这个过程是递归进行的，也就是说被包含的文件里面的 “#include” 也会处理
  删除所有的注释
  添加行号和文件名标识，比如 #2 “main.c” 2,以便编译时编译器产生的错误或者警告能够在控制台输出行号
  保留所有的 #pragma 编译器指令，因为编译器需要使用它们。
  预编译 命令： `gcc -E main.c -o main.i`

- 编译
  编译过程就是把预处理完的文件进行一系列的词法分析，语法分享，语义分析和优化后生成相应的汇编代码文件，这个过程往往是我们所说的整个程序构建的核心部分，也是最复杂的部分之一。

  编译命令：`gcc -S main.c -o main.s`
  执行完后得到汇编代码的输出文件main.s.

- 汇编
  汇编器是将汇编代码转变成机器可以执行的指令，每一个汇编语句几乎都对应一条机器指令。所以汇编器的汇编过程相对于编译器来说比较简单那，他没有复杂的语法，也没有语义，也不需要作指令优化。只是根据汇编指令和机器指令的对照表一一翻译就行了。汇编器输出目标文件。

  汇编命令 `as main.s -o main.o `或者 `gcc -c main.s -o main.o`
  `gcc -c main.c -o hello.o`

- 链接
  链接通常是一个让人费解的过程，为什么汇编器不直接输出可执行文件而是输出一个目标文件。

  很久之前，人们开发代码都是在一个文件里面，然后那个文件里面的代码随着功能的增多会变得异常臃肿，导致维护很困难。于是后面的模块化编程方式就出现了。若干个变量和函数组成一个模块，存放在一个".c"的源代码文件里，然后这些源代码文件按照目录结构来组织。在一个软件程序被分割成很多个模块后，就需要解决模块之间如何通信的问题，一种是模块见得函数调用，另一种是模块间的变量访问。函数访问需要知道目标函数的地址，变量访问也需要知道目标变量的地址，所以两种方式可以归结为一种方式，那就是模块间符号的应用。这个模块间的拼接过程就是链接

## 不同后缀的文件名

- .i后缀 ： 预编译，处理注释和预编译指令得到预编译文件。gcc -E main.c -o main.i

- .s后缀 ： 编译，通过语法分析和优化得到汇编代码。gcc -S main.c -o main.s`

- .o后缀：汇编，将汇编代码转为机器执行指令。 gcc -c main.s -o main.o

  .o文件是源码编译出的二进制文件

- .a后缀：实质上就是\*.o文件打了个包。一般把它叫做静态库文件。

- .so后缀：也是一系列.o文件的打包。一般叫动态库文件，是一个ELF格式的文件，即linux的可执行文件

- .out后缀：可执行文件。

linux上 对应windows : 

1. `.out / .exe `
2. `.o/.obj`
3. `.so/.dll`

## 动态库和静态库的区别

- **静态链接 (Static Linking)**：在这种方式下，所有的库函数都会被复制到最终的可执行文件中。这意味着，如果有多个程序使用同一个库，那么这个库的代码会在每个程序中都有一个副本。
- **动态链接 (Dynamic Linking)**：与静态链接相反，动态链接不会将库的代码复制到可执行文件中。相反，它只是在运行时加载所需的库。这样，多个程序可以共享同一个库的单一副本。



## 编译选项

ref:

https://zhuanlan.zhihu.com/p/661280913

### -fPIC 

动态链接库载入时重定位

ref:https://blog.csdn.net/foxriver_gjg1989/article/details/104432575

### -Wl

-Wl,-Bstatic 表示后面的库需要静态链接，-Wl,-Bdynamic 表示后面的库需要动态链接。

-Wl是告诉编译器把后面的参数传递给链接器(ld)。因为在编译 C++ 程序的时候，编译器(g++)调用了一个叫链接器(ld)的程序，它的作用是把各种对象文件、库文件链接在一起，生成可执行文件。
-Wl就是为了把编译器的参数传递给链接器，相当于直接传递给链接器一样，其后的参数都是传递给链接器的选项。例如，-Wl,-rpath-link，-Wl,-Bdynamic等选项，都是用来控制链接器行为的选项。

**-L**：指明库文件的目录；

**-l（小写的 'L'）**：指明库文件的名称；

**-I（大写的 'i'）**：指明头文件的目录；



### **RPATH**

RPATH（Runtime Library Path，运行时库路径）是一个在可执行文件或共享库中嵌入的路径，它指定了动态链接器在运行时应该搜索的目录。

例如，考虑以下情况：你有一个程序`myprog`，它依赖于`libfoo.so`这个库。这个库位于`/opt/mylibs/`目录下，而这个目录并不在系统的默认库搜索路径中。通过在`myprog`中设置RPATH为`/opt/mylibs/`，你可以确保`myprog`在运行时能够找到`libfoo.so`。

```cpp
// myprog.cpp
#include <foo.h>

int main() {
    foo_function();
    return 0;
}
```

在编译时，你可以使用以下命令来设置RPATH：

```bash
g++ myprog.cpp -L/opt/mylibs/ -lfoo -Wl,-rpath,/opt/mylibs/
```

### **RUNPATH**

RUNPATH（运行路径）也是一个在可执行文件或共享库中嵌入的路径。但与RPATH不同的是，当设置了RUNPATH时，动态链接器会忽略LD_LIBRARY_PATH环境变量。这意味着，如果你的程序设置了RUNPATH，那么即使你在环境变量中指定了正确的库路径，动态链接器也不会使用它。

### **$ORIGIN标记**

`$ORIGIN`是一个特殊的标记，它在动态链接时表示可执行文件或共享库的当前路径。

### **$LIB标记**

`$LIB`是动态链接器使用的一个特殊标记，它用于指定共享库的搜索路径。在多架构系统中，`$LIB`可以被替换为特定架构的目录，例如`lib32`（32位系统）或`lib64`（64位系统）。

这意味着，当一个应用程序在64位系统上运行时，动态链接器会自动搜索`lib64`目录下的共享库，而在32位系统上则会搜索`lib32`目录。

考虑以下目录结构：

```text
/myapp/
|-- myapp_executable
|-- lib32/
    |-- libfoo.so
|-- lib64/
    |-- libfoo.so
```

为了确保`myapp_executable`在不同的架构上都能找到正确的`libfoo.so`，我们可以在编译时设置RPATH为`$ORIGIN/$LIB`：

```bash
g++ myapp_executable.cpp -L./$LIB -lfoo -Wl,-rpath,'$ORIGIN/$LIB'
```

这样，`myapp_executable`会根据其运行的系统架构自动选择`lib32`或`lib64`目录。



### $PLATFORM

`$PLATFORM`是一个由动态链接器解析的特殊变量，它表示正在运行的系统的硬件平台。这个标记的主要目的是允许开发者为不同的硬件平台提供特定的共享库版本，而不需要更改应用程序的代码或设置。

例如，一个应用程序可能需要在x86和ARM两种架构上运行。通过使用`$PLATFORM`，开发者可以为每种架构提供一个特定的共享库版本，而应用程序可以在启动时动态地选择正确的版本。

假设我们有以下目录结构：

```text
/myapp/
|-- myapp_executable
|-- lib/
    |-- x86/
        |-- libfoo.so
    |-- arm/
        |-- libfoo.so
```

我们希望`myapp_executable`能够根据其正在运行的硬件平台自动选择正确的`libfoo.so`版本。

为了实现这一点，我们可以在编译`myapp_executable`时设置RPATH为`$ORIGIN/lib/$PLATFORM`：

```bash
g++ myapp_executable.cpp -L./lib/$PLATFORM -lfoo -Wl,-rpath,'$ORIGIN/lib/$PLATFORM'
```

这样，当`myapp_executable`在x86架构上运行时，它会加载`lib/x86/libfoo.so`，而在ARM架构上运行时，它会加载`lib/arm/libfoo.so`。



> 程序如何找到依赖库的文件？
>
> 动态链接器的搜索路径?

### 安装第三方库后如何配置库搜索路径？

- 默认搜索路径，例如/lib、/usr/lib等。这些路径在系统上通常都有一些常用的动态链接库。

- `LD_LIBRARY_PATH`是一个环境变量，它告诉动态链接器在哪里查找动态链接库。

```bash
export LD_LIBRARY_PATH=/path/to/lib:$LD_LIBRARY_PATH
```

最好在编译时通过rpath进行设置，

- 在/etc/ld.so.conf 文件中添加库的搜索路径，然后`ldconfig`命令来更新链接器的缓存（将/etc/ld.so.conf 文件列出的路径下的库文件缓存到/etc/ld.so.cache 以供使用）。

比如，默认情况下，编译器只会使用/lib 和/usr/lib这两个目录下的库文件。通常默认的一些库文件源码会包含.configure文件来帮助编译，编译安装文件如果不指定一般默认放在/usr/local目录下，那么就需要在/etc/ld.so.conf 文件中添加/usr/local/lib 路径，并使用`ldconfig`命令更新库缓存。

还可以到/etc/ld.so.conf.d 目录下添加libname.conf的文件, 在该文件中添加lib库的绝对路径，然后保存.conf文件，使用命令`sudo ldconfig` 刷新库缓存。

- pkg_config

pkg_config是一个用于管理库文件的工具。帮助configure程序提供软件版本，库版本，库路径等信息,以便在编译期间使用. pkg_config详细介绍[here](https://blog.csdn.net/qq_36182852/article/details/109680418)and[here](https://zhuanlan.zhihu.com/p/661282452)。

通常支持pkg_config的库在编译安装完成后会在lib目录下有一个pkg-config的文件夹，其中包含一个.pc文件，文件中记录了库安装的路径，版本等信息。

将.pc的 文件所在的目录路径添加到PKG_CONFIG_PATH变量。可以加到~/.bashrc中

```bash
PKG_CONFIG_PATH=/usr/local/libname/lib/pkgconfig:/usr/lib/pkgconfig
```

然后`source ~/.bashrc`更新环境变量。

**总结：**

通常情况下安装库，可以通过以下方式配置库文件的搜索路径。

1. 如果支持pkgc-config，那么可以通过添加PKG_CONFIG_PATH环境变量的方式指定.pc文件的位置，从而给出库文件的搜索路径。
2. 在/etc/ld.so.conf.d  下添加libname.conf文件然后ldconfig刷新链接器缓存。
3. 修改环境变量LD_LIBRARY_PATH 列出库路径，然后source。



## cmake 中调用第三方库

配置库的搜索路径。

交叉编译时的第三方库怎么引入？

比如本地交叉编译项目A， 项目A 依赖项目B的库, 项目B 依赖项目C的库.



### **为什么有的程序依赖第三方库也可以直接运行，有的需要通过export LD_LIBRARY_PATH 指定库路径了才能运行？**

使用 -rpath 时，其**指定的路径会被包含在生成的目标文件中**（可执行程序或动态库），而 -rpath-link 只在**链接**时使用。

**-rpath 指定的路径会被包含在生成的目标文件中**，这有什么用呢？

如果在目标文件中包含了动态库的路径，那么在执行目标程序之前就**无需**指定动态库的路径了。反之则需要使用 `export LD_LIBRARY_PATH=XXX` 指定动态库的路径。



**CMake在默认情况下是会给你的exe加入相关RPATH的**

https://blog.csdn.net/u011436427/article/details/126232321



# 交叉编译

**1.交叉编译的概念**

交叉编译，是一个和，本地编译，相对应的概念。交叉编译是在一个平台上生成另一个平台上的可执行代码。

交叉编译，这个概念，主要和嵌入式开发有关。

> 例如：在x86平台上编译，在ARM平台上运行。一种最常见的例子就是：
>
> 在进行嵌入式开发时手上有个嵌入式开发板，CPU是arm的然后在x86的平台下开发，比如Ubuntu的Linux，或者是Win 10然后就需要：
>
> 1.在x86的平台上，（用交叉编译器）去编译你写好的程序代码
>
> 2.编译生成的（可执行的）程序，是放到目标开发板，arm的CPU上运行的
>
> 此所谓：在x86平台上编译，在ARM平台上运行。

**2.为何要有交叉编译**
之所以要有交叉编译，主要原因是：嵌入式系统中的资源太少

具体的解释就是：交叉编译出来的程序，所要运行的目标环境中，各种资源，都相对有限，所以很难进行直接的本地编译。


## 交叉编译器

ref:https://www.cnblogs.com/lixuejian/p/12848728.html

在使用3588板子交叉编译时遇到的交叉编译器有

```
aarch64-linux-gnu
arm-rockchip830-linux-uclibcgnueabihf
```

还有一些其他的交叉编译器

```
arm-linux-gcc
arm-linux-gnueabi-gcc
arm-none-linux-eabi-gcc
arm-none-symbianelf-gcc
arm-none-uclinuxeabi-gcc
arm-none-linux-gnueabi-gcc
arm-cortex_a8-linux-gnueabi-gcc
mips-malta-linux-gnu-gcc
```

> Q:
>
> - 为什么会有这些不同的交叉编译器，他们的区别在哪里？分别用在什么场合？
>
> 根据其命名，不同的交叉编译器，可能会应用与不同的平台架构，比如ARM, MIPS。是否支持操作系统，或者支持指定的操作系统，比如linux。来自于不同的硬件厂商编译得到。



### 交叉编译工具链命名规则

交叉编译工具链的命名规则为：**arch [-vendor] [-os] [-(gnu)eabi]**

- arch - 体系架构，如ARM，MIPS

- vendor - 工具链提供商

- os - 目标操作系统

- eabi - 嵌入式应用二进制接口（Embedded Application Binary Interface）

根据对操作系统的支持与否，ARM GCC可分为支持和不支持操作系统，如

- arm-none-eabi：这个是没有操作系统的，自然不可能支持那些跟操作系统关系密切的函数，比如fork(2)。他使用的是newlib这个专用于嵌入式系统的C库。
- arm-none-linux-eabi：用于Linux的，使用Glibc

### 几个交叉编译器的介绍

- arm-none-linux-gnueabi-gcc：是 Codesourcery 公司（目前已经被Mentor收购）基于GCC推出的的ARM交叉编译工具。可用于交叉编译ARM（32位）系统中所有环节的代码，包括裸机程序、u-boot、Linux kernel、filesystem和App应用程序。

- arm-linux-gnueabihf-gcc：是由 Linaro 公司基于GCC推出的的ARM交叉编译工具。可用于交叉编译ARM（32位）系统中所有环节的代码，包括裸机程序、u-boot、Linux kernel、filesystem和App应用程序。

- aarch64-linux-gnu-gcc：是由 Linaro 公司基于GCC推出的的ARM交叉编译工具。可用于交叉编译ARMv8 64位目标中的裸机程序、u-boot、Linux kernel、filesystem和App应用程序。

- arm-none-elf-gcc：是 Codesourcery 公司（目前已经被Mentor收购）基于GCC推出的的ARM交叉编译工具。可用于交叉编译ARM MCU（32位）芯片，如ARM7、ARM9、Cortex-M/R芯片程序。

- arm-none-eabi-gcc：是 GNU 推出的的ARM交叉编译工具。可用于交叉编译ARM MCU（32位）芯片，如ARM7、ARM9、Cortex-M/R芯片程序。

 ### **ABI 和 EABI**

**ABI**：二进制应用程序接口(Application Binary Interface (ABI) for the ARM Architecture)。在计算机中，应用二进制接口描述了应用程序（或者其他类型）和操作系统之间或其他应用程序的低级接口。

**EABI**：嵌入式ABI。嵌入式应用二进制接口指定了文件格式、数据类型、寄存器使用、堆积组织优化和在一个嵌入式软件中的参数的标准约定。开发者使用自己的汇编语言也可以使用 EABI 作为与兼容的编译器生成的汇编语言的接口。

两者主要区别是，ABI是计算机上的，EABI是嵌入式平台上（如ARM，MIPS等）。

### 不同的交叉编译器结果不一致问题

做项目时遇到同一套代码本地电脑的交叉编译器编译的结果能够正常运行，板端的交叉编译器编译结果异常的问题.

那么**不同的编译器、不同的硬件平台以及不同的操作系统，最终编译出来的结果一样吗？为什么？**

对于不同的编译器，整个流程（预处理——编译器（词法分析、语法分析，语义分析...)——汇编器——链接器)之中只要有稍微一点的不同，我想编译后的结果——可执行文件都是不同的。

对于不同的硬件平台，比如x86、SPARC、MIPS、ARM等，它们的寻址方式、地址格式、指令格式等等等等都不相同，那么编译的过程必然也会有所不同，结果自然不同。

对于不同的操作系统，答案是一目了然的。不同的操作系统下，它的可执行文件格式的要求都不相同，共享库以及动态链接方式都不一样，那么结果肯定也就不一样的啦。

## 如何进行交叉编译

### 交叉编译工具安装。

根据需求下载合适的交叉编译器到本地，配置交叉工具环境， 将交叉编译器路径配置到~/.bashrc下的$PATH 中，使其生效。配置好后，假设交叉编译器为:aarch64-linux-gnu , 命令行输入

```bash
aarch64-linux-gnu-gcc -v 
```

可以看到输出版本信息。

### 编译时配置交叉工具

#### cmake编译时指定交叉编译器的方法

1.

```bash
cmake -D CMAKE_C_COMPILER=aarch64-linux-gnu-gcc \
      -D CMAKE_CXX_COMPILER=aarch64-linux-gnu-g++ ..   
```

2.

也可以通过脚本

```bash
GCC_COMPILER=aarch64-linux-gnu
export CC=${GCC_COMPILER}-gcc
export CXX=${GCC_COMPILER}-g++
cmake ..
```

3.

使用cmake-gui 。在cmakesetup中选择最下面的`Specify options for cross-compiling` 选项。然后在接下来的页面中， 在compiler下的c 和c++ 项目中输入交叉编译的路径，比如'/bin/aarch64-linux-gnu-gcc'.'/bin/aarch64-linux-gnu-g++'。



# 常见动态库相关问题

## **找不到库文件**

动态库的路径问题，程序在运行时找不到所需的动态库，那么可能是因为这个库不在动态链接器的搜索路径中。
在Linux中，动态链接器搜索动态库的路径包括以下几个部分：

- 编译时指定的RPATH（Runtime Library Search Path）
- 环境变量LD_LIBRARY_PATH
- 配置文件/etc/ld.so.conf中指定的路径
- 默认的库路径，如/usr/lib和/lib



undefined reference to找不到符号问题汇总及解决方法

https://blog.csdn.net/bandaoyu/article/details/114655884

## 库相关命令

### ldd 命令

在解决动态库问题时，ldd（List Dynamic Dependencies）命令可以列出一个程序所依赖的所有动态库，以及这些库的路径。例如，你可以使用以下命令来检查你的程序：

```
ldd your_program
```

这个命令会输出你的程序所依赖的所有动态库，以及这些库的路径。如果某个库找不到，ldd会显示"not found"。

### nm命令

nm命令是linux下自带的特定文件分析工具，一般用来检查分析二进制文件、库文件、可执行文件中的符号表，返回二进制文件中各段的信息。

```
nm your_program
```

ref:https://blog.csdn.net/D_ovis/article/details/135052933

### readelf命令

用于显示ELF格式文件的信息，包括可执行文件、共享库、目标代码等。它支持32位和64位的ELF文件，并且可以显示文件头、节头、程序头、符号表等信息。



